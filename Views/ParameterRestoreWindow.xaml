<Window x:Class="ViewTracker.Views.ParameterRestoreWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Restore Parameters from Snapshot"
        Height="700" Width="1100"
        WindowStartupLocation="CenterScreen"
        Background="#F5F5F5">

    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>

        <Style TargetType="CheckBox">
            <Setter Property="Margin" Value="5,3"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>

        <!-- Style for type parameter checkboxes (grayed out) -->
        <Style x:Key="TypeParameterCheckBoxStyle" TargetType="CheckBox">
            <Setter Property="Margin" Value="5,3"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="IsEnabled" Value="False"/>
            <Setter Property="Foreground" Value="#999"/>
        </Style>
    </Window.Resources>

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#2196F3" Padding="15" CornerRadius="5" Margin="0,0,0,15">
            <StackPanel>
                <TextBlock x:Name="HeaderTitle" Text="Restore Parameters from Snapshot" FontSize="20" FontWeight="Bold" Foreground="White"/>
                <TextBlock Text="Select a version and instance parameters to restore" FontSize="12" Foreground="White" Margin="0,5,0,0"/>
            </StackPanel>
        </Border>

        <!-- Main Content: Three Columns -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="350"/>
                <ColumnDefinition Width="300"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- LEFT COLUMN: Options -->
            <ScrollViewer Grid.Column="0" VerticalScrollBarVisibility="Auto" Margin="0,0,10,0">
                <StackPanel>
                    <!-- Version Selection -->
                    <GroupBox Header="Select Version" Padding="10" Margin="0,0,0,10">
                        <StackPanel>
                            <ComboBox x:Name="VersionComboBox"
                                      DisplayMemberPath="DisplayName"
                                      SelectionChanged="VersionComboBox_SelectionChanged"
                                      Padding="8" FontSize="13"/>
                            <TextBlock x:Name="VersionInfoText"
                                       Margin="5,8,0,0"
                                       FontSize="11"
                                       Foreground="#666"
                                       TextWrapping="Wrap"/>
                        </StackPanel>
                    </GroupBox>

                    <!-- Scope -->
                    <GroupBox Header="Scope" Padding="10" Margin="0,0,0,10">
                        <StackPanel>
                            <RadioButton x:Name="AllElementsRadio" Content="All elements with trackID" IsChecked="True"
                                         GroupName="Scope" Checked="ScopeRadio_Changed"/>
                            <RadioButton x:Name="SelectedElementsRadio" Content="Pre-selected elements only"
                                         GroupName="Scope" Margin="0,5,0,0" Checked="ScopeRadio_Changed"/>
                            <TextBlock x:Name="ScopeInfoText" Margin="20,5,0,0" FontSize="11" Foreground="#666"/>
                        </StackPanel>
                    </GroupBox>

                    <!-- Status Info -->
                    <Border Background="White" BorderBrush="#DDD" BorderThickness="1"
                            Padding="10" CornerRadius="3" Margin="0,0,0,10">
                        <ScrollViewer MaxHeight="120" VerticalScrollBarVisibility="Auto">
                            <StackPanel x:Name="StatusPanel">
                                <TextBlock Text="Status:" FontWeight="Bold" Margin="0,0,0,5"/>
                                <TextBlock x:Name="StatusText" TextWrapping="Wrap" FontSize="12"/>
                            </StackPanel>
                        </ScrollViewer>
                    </Border>

                    <!-- Options -->
                    <GroupBox Header="Options" Padding="10" Margin="0,0,0,10">
                        <StackPanel>
                            <CheckBox x:Name="ChkCreateBackup" Content="Create backup snapshot before restoring"
                                      IsChecked="True" FontWeight="Bold"/>
                        </StackPanel>
                    </GroupBox>
                </StackPanel>
            </ScrollViewer>

            <!-- MIDDLE COLUMN: Parameters -->
            <GroupBox Grid.Column="1" Header="Select Parameters to Restore" Padding="10" Margin="0,0,10,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,8">
                        <Button Content="Select All" Click="SelectAllInstance_Click" Padding="10,5" FontSize="12"/>
                        <Button Content="Select None" Click="SelectNone_Click" Padding="10,5" FontSize="12"/>
                    </StackPanel>

                    <!-- Scrollable parameter list -->
                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                        <StackPanel x:Name="InstanceParameterPanel">
                            <!-- Dynamically populated with instance parameter checkboxes -->
                        </StackPanel>
                    </ScrollViewer>

                    <TextBlock Grid.Row="2"
                               Text="Note: Only instance parameters are shown. Type parameters cannot be restored."
                               Margin="0,10,0,0" FontSize="11" Foreground="#666" TextWrapping="Wrap"/>
                </Grid>
            </GroupBox>

            <!-- RIGHT COLUMN: Element List with Parameter Preview -->
            <GroupBox x:Name="ElementListGroupBox" Grid.Column="2" Header="Elements to Restore" Padding="10">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,8">
                        <Button Content="Select All" Click="SelectAllElements_Click" Padding="10,5" FontSize="12"/>
                        <Button Content="Select None" Click="SelectNoneElements_Click" Padding="10,5" FontSize="12"/>
                    </StackPanel>

                    <!-- Scrollable element list -->
                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                        <ItemsControl x:Name="ElementsItemsControl">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border BorderBrush="#DDD" BorderThickness="1" Margin="0,0,0,8"
                                            Padding="8" CornerRadius="3" Background="White">
                                        <StackPanel>
                                            <CheckBox Content="{Binding ElementDisplayName}"
                                                      IsChecked="{Binding IsSelected, Mode=TwoWay}"
                                                      FontWeight="Bold" FontSize="13"/>
                                            <TextBlock Text="{Binding ElementInfo}"
                                                       Margin="20,2,0,4" FontSize="11" Foreground="#666"/>

                                            <!-- Type mismatch warning -->
                                            <TextBlock Text="{Binding TypeMismatchText}"
                                                       Visibility="{Binding TypeMismatchVisibility}"
                                                       Margin="20,4,0,4" FontSize="11" FontWeight="SemiBold"
                                                       Foreground="#FF9800" TextWrapping="Wrap"/>

                                            <!-- Type parameter warnings -->
                                            <TextBlock Text="{Binding TypeParameterWarningsText}"
                                                       Visibility="{Binding TypeParameterWarningsVisibility}"
                                                       Margin="20,4,0,4" FontSize="11" FontWeight="SemiBold"
                                                       Foreground="#FF5722" TextWrapping="Wrap"/>

                                            <!-- Parameter preview -->
                                            <TextBlock Text="Parameters to restore:"
                                                       Margin="20,6,0,2" FontSize="11" FontWeight="SemiBold"
                                                       Visibility="{Binding HasParametersVisibility}"/>
                                            <ItemsControl ItemsSource="{Binding ParameterPreview}"
                                                          Margin="20,0,0,0">
                                                <ItemsControl.ItemTemplate>
                                                    <DataTemplate>
                                                        <TextBlock FontSize="10" Foreground="#555">
                                                            <Run Text="{Binding Name}" FontWeight="SemiBold"/>: <Run Text="{Binding Value}"/>
                                                        </TextBlock>
                                                    </DataTemplate>
                                                </ItemsControl.ItemTemplate>
                                            </ItemsControl>
                                            <TextBlock Text="(No parameters selected)"
                                                       Visibility="{Binding HasNoParameters}"
                                                       Margin="20,0,0,0" FontSize="10" Foreground="#999" FontStyle="Italic"/>
                                        </StackPanel>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </Grid>
            </GroupBox>
        </Grid>

        <!-- Action Buttons -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Preview Changes..." Click="Preview_Click" Background="#2196F3" Foreground="White"/>
            <Button Content="Cancel" Click="Cancel_Click" Background="#757575" Foreground="White"/>
            <Button Content="Restore" Click="Restore_Click" Background="#4CAF50" Foreground="White" FontWeight="Bold"/>
        </StackPanel>
    </Grid>
</Window>
