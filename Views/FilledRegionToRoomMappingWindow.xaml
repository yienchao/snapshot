<Window x:Class="ViewTracker.Views.FilledRegionToRoomMappingWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Map Filled Region Parameters to Room Parameters"
        Height="700" Width="750"
        WindowStartupLocation="CenterScreen"
        Background="#F5F5F5">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#2196F3" Padding="15" CornerRadius="5" Margin="0,0,0,15">
            <StackPanel>
                <TextBlock Text="Convert Filled Regions to Rooms" FontSize="18" FontWeight="Bold" Foreground="White"/>
                <TextBlock Text="Map filled region parameters to room parameters" FontSize="12" Foreground="White" Margin="0,5,0,0"/>
            </StackPanel>
        </Border>

        <!-- Info Section -->
        <Border Grid.Row="1" Background="#FFF9C4" BorderBrush="#FFD54F" BorderThickness="1" CornerRadius="3" Padding="10" Margin="0,0,0,10">
            <StackPanel>
                <TextBlock x:Name="InfoText" TextWrapping="Wrap" FontSize="12"/>
            </StackPanel>
        </Border>

        <!-- Mapping Grid -->
        <Border Grid.Row="2" Background="White" BorderBrush="#DDD" BorderThickness="1" CornerRadius="3" Padding="10">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="280"/>
                            <ColumnDefinition Width="30"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Column="0" Text="Filled Region Parameter" FontWeight="Bold" Margin="5"/>
                        <TextBlock Grid.Column="2" Text="Room Parameter" FontWeight="Bold" Margin="5"/>
                    </Grid>

                    <Separator Margin="0,5,0,10"/>

                    <!-- Dynamic mapping rows -->
                    <ItemsControl x:Name="MappingItemsControl">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Grid Margin="0,5">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="280"/>
                                        <ColumnDefinition Width="30"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>

                                    <TextBlock Grid.Column="0" Text="{Binding SourceParameter}" VerticalAlignment="Center" Margin="5" FontWeight="SemiBold"/>
                                    <TextBlock Grid.Column="1" Text="→" VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="16" Foreground="#666"/>
                                    <ComboBox Grid.Column="2" ItemsSource="{Binding AvailableRoomParameters}" SelectedItem="{Binding TargetParameter}" Margin="5"/>
                                </Grid>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </ScrollViewer>
        </Border>

        <!-- Conversion Options -->
        <Border Grid.Row="3" Background="White" BorderBrush="#DDD" BorderThickness="1" CornerRadius="3" Padding="10" Margin="0,10,0,10">
            <StackPanel>
                <TextBlock Text="Conversion Options" FontWeight="Bold" Margin="0,0,0,8"/>
                <CheckBox x:Name="ChkPlaceAtCentroid" Content="Place rooms at filled region centroid" IsChecked="True" Margin="0,3"/>
                <CheckBox x:Name="ChkDeleteFilledRegions" Content="Delete filled regions after conversion" IsChecked="False" Margin="0,3"/>
                <CheckBox x:Name="ChkAddTrackID" Content="Add trackID parameter to created rooms" IsChecked="True" Margin="0,3"/>

                <Separator Margin="0,8"/>

                <TextBlock Text="Preset Management" FontWeight="Bold" Margin="0,8,0,5"/>
                <StackPanel Orientation="Horizontal">
                    <Button x:Name="BtnSavePreset" Content="Save as Preset..." Click="SavePreset_Click" Padding="10,5" Margin="0,0,5,0"/>
                    <Button x:Name="BtnLoadPreset" Content="Load Preset..." Click="LoadPreset_Click" Padding="10,5"/>
                    <TextBlock x:Name="PresetStatusText" VerticalAlignment="Center" Margin="10,0,0,0" Foreground="#666" FontStyle="Italic"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- Action Buttons -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancel" Click="Cancel_Click" Padding="20,8" Margin="5" Background="#757575" Foreground="White"/>
            <Button Content="Convert" Click="Convert_Click" Padding="20,8" Margin="5" Background="#4CAF50" Foreground="White" FontWeight="Bold"/>
        </StackPanel>
    </Grid>
</Window>
